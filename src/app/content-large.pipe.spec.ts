import { ContentLargePipe } from './content-large.pipe';

describe('ContentLargePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentLargePipe();
    expect(pipe).toBeTruthy();
  });
});
